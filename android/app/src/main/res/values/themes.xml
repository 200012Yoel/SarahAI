<resources>
    <!-- Thème ultra-compatible AppCompat pour éviter tout crash de démarrage -->
    <style name="Theme.SarahIA" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/sarah_bg</item>
        <item name="android:colorBackground">@color/sarah_bg</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <style name="Theme.SarahIA.FullScreen" parent="Theme.SarahIA">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
